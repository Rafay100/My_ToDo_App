apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-ai-platform.fullname" . }}-audit-service-svc
  labels:
    {{- include "todo-ai-platform.labels" . | nindent 4 }}
    app: audit-service
spec:
  type: {{ .Values.auditService.service.type }}
  ports:
    - port: {{ .Values.auditService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-ai-platform.selectorLabels" . | nindent 4 }}
    app: audit-service